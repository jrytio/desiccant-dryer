# The screen's drawing code (components/dryer_ui) from this checkout. Every
# selector uses this.
external_components:
  - source:
      type: local
      path: components
    components: [dryer_ui]

dryer_ui:
